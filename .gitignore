# Dependencies
node_modules/

# Next.js
.next/
out/

# Environment — never commit secrets
.env.local
.env*.local

# Local cache — these are machine-specific, not source-controlled
.cache/

# Build artifacts
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

.vercel
.env*.local

# scroll-world build pipeline — 623MB incl. node_modules; archived to Drive, not versioned
world-build/
