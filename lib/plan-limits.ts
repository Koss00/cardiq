// Shared plan limits — imported by both server (gates.ts) and client (UpgradeModal).
// This file must NOT import any server-side modules.
export const FREE_CARD_LIMIT   = 5;
export const FREE_SIGNAL_LIMIT = 20;
